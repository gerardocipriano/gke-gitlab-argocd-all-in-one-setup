# Base di kustomize per l'applicazione demo.
# Definisce Deployment e Service; il tag immagine viene riscritto da Kargo ad ogni promozione.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
images:
  - name: public.ecr.aws/nginx/nginx
    newTag: "1.26.0"
